{
  "name": "mermaid-importer-plugin",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "vite build --watch",
    "build": "tsc && vite build",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5",
    "vite": "^5",
    "vitest": "^2",
    "@penpot/plugin-types": "^1"
  },
  "dependencies": {
    "mermaid": "^11",
    "@penpot/plugin-styles": "^1"
  }
}